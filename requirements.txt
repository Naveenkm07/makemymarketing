# Core SaaS stack for makemymarketing
# This file mirrors the npm dependencies in web/package.json for reference

# IMPORTANT:
# This repository is a Node.js/Next.js project. Netlify treats a requirements.txt
# as Python dependencies and runs `pip install -r requirements.txt`.
#
# To prevent Netlify build failures, keep this file as documentation-only.
# Do NOT add npm packages here.
